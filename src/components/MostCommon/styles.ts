import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => {
  return {
    list: {
      width: 250,
    },
    fullList: {
      width: "auto",
    },
    btn: {
      margin: 10,
    },
  };
});
